import { Metadata } from "next"

export const metadata : Metadata = {
  title: 'MindStudent - Upload de Arquivos',
  description: '...',
}

export default function DriveLayout({ children }: { children: React.ReactNode }) {
  return children
}