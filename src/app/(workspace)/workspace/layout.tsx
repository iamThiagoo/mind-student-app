import { Metadata } from "next"

export const metadata : Metadata = {
  title: 'MindStudent - Workspace',
  description: '...',
}

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children
}