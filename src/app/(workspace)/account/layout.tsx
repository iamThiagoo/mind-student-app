import { Metadata } from "next"

export const metadata : Metadata = {
  title: 'MindStudent - Minha Conta',
  description: '...',
}

export default function AccountLayout({ children }: { children: React.ReactNode }) {
  return children
}