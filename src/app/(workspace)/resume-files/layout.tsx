import { Metadata } from "next"

export const metadata : Metadata = {
  title: 'MindStudent - Resumo de Arquivos',
  description: '...',
}

export default function ResumeFilesLayout({ children }: { children: React.ReactNode }) {
  return children
}